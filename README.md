# Wikipedia-Bias-Detection
A full-stack NLP application that detects bias and sentiment in Wikipedia articles using a trained TF-IDF + Logistic Regression classifier, VADER sentiment model, and paragraph-level analysis. The web interface is built using Flask.
